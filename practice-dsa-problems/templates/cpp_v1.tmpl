#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <algorithm>
#include "nlohmann/json.hpp"

using namespace std;
using json = nlohmann::json;

// ========== USER CODE ==========
${USER_CODE}

// ========== ADAPTER ==========
${ADAPTER}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    try {
        string input;
        getline(cin, input, '\0');

        json data = json::parse(input);
        json args = data["args"];

        json result = adapter(args);

        cout << result.dump() << endl;
    } catch (const exception& e) {
        cerr << "Runtime Error: " << e.what() << endl;
        return 1;
    }

    return 0;
}
